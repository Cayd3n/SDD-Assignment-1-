if (cursor=="|") //Checks cursor display every 20 steps
{
	cursor = ""; //Changes cursor display to empty
}
else
{
	cursor = "|"; //Changes the cursor display back to original
}

alarm[0] = blinkdelay; //Reinitialise alarm back to 20 steps





